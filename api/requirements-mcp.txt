# MCP server dependencies — isolated from api/ingest so mcp's newer httpx
# requirement doesn't collide with their httpx==0.27.0 pin. The MCP server only
# needs the analysis tool registry (no httpx / fastapi). mcp brings starlette +
# uvicorn + anyio + pydantic transitively.
mcp==1.27.2
psycopg[binary]==3.1.18
jsonschema==4.21.1
python-dateutil==2.9.0
